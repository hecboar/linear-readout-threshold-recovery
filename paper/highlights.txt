Highlights (Elsevier: 3-5 bullets, each <= 85 characters including spaces)

- A Welch-type floor bounds the interference-to-gain ratio of any linear readout.
- We also compute a given code's own floor, splitting geometry from decoder quality.
- One linear programme gives a feature's largest affinely detectable sparsity.
- Low leverage forces that limit down; the converse fails, so the hierarchy is strict.
- Gradient-optimised codes reach the floor; the tied variant becomes a tight frame.

Character counts must be re-checked after any edit:
  python -c "print(*((len(l[2:].rstrip()), l[2:].rstrip()) for l in open('highlights.txt') if l.startswith('- ')), sep='\n')"
